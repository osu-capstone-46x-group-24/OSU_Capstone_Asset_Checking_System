import type { Config } from "tailwindcss";

const config: Config = {
    content: [
    ],
    theme: {
        extend: {
            colors: {
            }
        }
    },
    plugins: [],
    darkMode: "class"
};

export default config;